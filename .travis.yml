language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gowebapp' ]; then cd .. && eval "mv $currentfolder generator-gowebapp" && cd generator-gowebapp; fi

